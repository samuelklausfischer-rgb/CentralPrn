// Placeholder de desenvolvimento.
// Em produção, o container SOBRESCREVE este arquivo com os valores reais das
// variáveis de ambiente (ver docker-entrypoint.d/40-config.sh).
// Em dev, fica vazio e o app usa import.meta.env (.env.local) via src/env.js.
window.__ENV__ = window.__ENV__ || {};
